import actionTypes from "../appActions";

/*const appReducer = ( state = initialState , action)=>{
    switch (action.type){
        case actionTypes.CHANGE_LANGUAGE:
            console.log("Minh : " , action);
            return{
                ...state,
            }
        default :  return 0;
    }
}*/